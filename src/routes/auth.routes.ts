import { Router } from "express";

const router = Router();

/**
 * ⚠️ DEPRECATED - Use /api/login (Unified Login) instead
 * This route is kept for backward compatibility only
 */

export default router;
